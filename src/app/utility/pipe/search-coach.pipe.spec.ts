import { SearchCoachPipe } from './search-coach.pipe';

describe('SearchCoachPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchCoachPipe();
    expect(pipe).toBeTruthy();
  });
});
