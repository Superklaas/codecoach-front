import { ContextBackgroundDirective } from './context-background.directive';

describe('ContextBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new ContextBackgroundDirective(null, null, null);
    expect(directive).toBeTruthy();
  });
});
